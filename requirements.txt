# Pinned for reproducible container builds. Exact versions match the tested venv
# (pip freeze); pip resolves transitives (pandas, dateutil, soupsieve, urllib3, …).
# boto3 floored (not installed locally yet) — pin it exactly after the first build.
Flask==3.1.3
requests==2.33.0
yfinance==1.2.0
APScheduler==3.11.2
pypdf==6.14.2
beautifulsoup4==4.14.3
numpy==2.4.3
pandas==3.0.1
pytz==2026.1
boto3>=1.35
